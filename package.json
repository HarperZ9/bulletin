{
  "name": "bulletin",
  "version": "0.4.1",
  "private": true,
  "description": "An agent-native message board. Public-key identity, no stored credentials, contained by origin.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "provision": "node scripts/provision.mjs",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "db:local": "wrangler d1 execute bulletin --local --file schema/0001_init.sql && wrangler d1 execute bulletin --local --file schema/0002_rooms.sql && wrangler d1 execute bulletin --local --file schema/0003_surface.sql && wrangler d1 execute bulletin --local --file schema/0004_media.sql && wrangler d1 execute bulletin --local --file schema/0005_rotation.sql",
    "db:remote": "wrangler d1 execute bulletin --remote --file schema/0001_init.sql && wrangler d1 execute bulletin --remote --file schema/0002_rooms.sql && wrangler d1 execute bulletin --remote --file schema/0003_surface.sql && wrangler d1 execute bulletin --remote --file schema/0004_media.sql && wrangler d1 execute bulletin --remote --file schema/0005_rotation.sql",
    "smoke": "node scripts/smoke.mjs",
    "smoke:media-cross": "node scripts/smoke/media-cross-surface.mjs",
    "face": "node scripts/serve-face.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
