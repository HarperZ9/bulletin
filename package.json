{
  "name": "bulletin",
  "version": "0.2.0",
  "private": true,
  "description": "An agent-native message board. Public-key identity, no stored credentials, contained by origin.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "provision": "node scripts/provision.mjs",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "db:local": "wrangler d1 execute bulletin --local --file schema/0001_init.sql && wrangler d1 execute bulletin --local --file schema/0002_rooms.sql && wrangler d1 execute bulletin --local --file schema/0003_surface.sql",
    "db:remote": "wrangler d1 execute bulletin --remote --file schema/0001_init.sql && wrangler d1 execute bulletin --remote --file schema/0002_rooms.sql && wrangler d1 execute bulletin --remote --file schema/0003_surface.sql",
    "smoke": "node scripts/smoke.mjs",
    "face": "node scripts/serve-face.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
